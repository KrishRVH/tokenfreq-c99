diff --git a/CMakeLists.txt b/CMakeLists.txt
index 6f4a2f7..a4fb192 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -80,7 +80,8 @@ function(wc_apply_common target)
     endif()
   else()
     target_compile_options(${target} ${_opt_scope}
-      -Wall -Wextra -Wpedantic -Wconversion -Wshadow
+      $<$<COMPILE_LANGUAGE:C>:-Wall -Wextra -Wpedantic -Wconversion -Wshadow>
+      $<$<COMPILE_LANGUAGE:CXX>:-Wall -Wextra -Wpedantic -Wconversion -Wshadow>
     )
     if(WC_WERROR)
       target_compile_options(${target} ${_opt_scope} -Werror)
@@ -240,6 +241,12 @@ endif()
 # =============================================================================
 
 if(BUILD_TESTING)
+  include(CheckLanguage)
+  check_language(CXX)
+  if(CMAKE_CXX_COMPILER)
+    enable_language(CXX)
+  endif()
+
   function(create_test_variant name lib)
     add_executable(${name} wc_test.c)
     target_link_libraries(${name} PRIVATE ${lib})
@@ -308,6 +315,35 @@ if(BUILD_TESTING)
     target_compile_options(wc_fuzz_diff PRIVATE -fsanitize=fuzzer-no-link)
     target_link_options(wc_fuzz_diff PRIVATE -fsanitize=fuzzer)
   endif()
+
+  function(wc_add_compile_only target src)
+    add_library(${target} OBJECT ${src})
+    wc_apply_common(${target})
+    if(MSVC)
+      target_compile_options(${target} PRIVATE /WX)
+    else()
+      target_compile_options(${target} PRIVATE -Werror)
+    endif()
+  endfunction()
+
+  wc_add_compile_only(wc_compile_static_init tests/compile_static_init.c)
+
+  if(CMAKE_CXX_COMPILER)
+    wc_add_compile_only(wc_compile_cpp tests/compile_cpp.cpp)
+    set_target_properties(wc_compile_cpp PROPERTIES
+      CXX_STANDARD 11
+      CXX_STANDARD_REQUIRED ON
+      CXX_EXTENSIONS OFF
+    )
+  else()
+    message(STATUS "C++ compiler not found; skipping C++ compile test.")
+  endif()
+
+  add_custom_target(wc_compile_checks ALL)
+  add_dependencies(wc_compile_checks wc_compile_static_init)
+  if(TARGET wc_compile_cpp)
+    add_dependencies(wc_compile_checks wc_compile_cpp)
+  endif()
 endif()
 
 if(WC_INSTALL)
diff --git a/README.md b/README.md
index 1763a7c..c6e7094 100644
--- a/README.md
+++ b/README.md
@@ -292,8 +292,12 @@ In this mode:
 
 When `WC_NO_HEAP=0` (default):
 
-* The handle (`struct wc`) itself and arrays returned by `wc_results()` /
-  `wc_topn()` are allocated via `WC_MALLOC` / `WC_FREE`.
+* Unless `wc_limits.handle_buf` is provided, the handle (`struct wc`) itself is
+  allocated via `WC_MALLOC` / `WC_FREE`.
+* Arrays returned by `wc_results()` / `wc_topn()` are allocated via
+  `WC_MALLOC` / `WC_FREE`.
+* `wc_stream_open()` allocates a small `wc_stream` object via `WC_MALLOC` /
+  `WC_FREE` (closed by `wc_stream_close()`).
 
 When `WC_NO_HEAP=1`:
 
@@ -302,7 +306,7 @@ When `WC_NO_HEAP=1`:
 * `wc_results` and `wc_topn` return `WC_NOMEM` unconditionally; use
   `wc_results_into` / `wc_topn_into` with caller-supplied storage, or
   iterate via `wc_cursor`.
-* `wc_stream_open` returns `NULL` with `WC_EBADLIMITS`; use
+* `wc_stream_open` returns `NULL` with `WC_NOMEM`; use
   `wc_stream_open_inplace` with caller-supplied storage.
 
 On very small systems with `WC_NO_HEAP=0`, redirect `WC_MALLOC` / `WC_FREE`
@@ -374,8 +378,9 @@ All public functions follow a clear error protocol:
   * Returns `WC_OK`, `WC_ERROR`, or `WC_NOMEM`
   * On `WC_OK` with no entries: `*out == NULL`, `*n == 0`
 * `wc_errstr` converts any result code into a human-readable static string.
-* `wc_open_ex` and `wc_stream_open` set `errno` on failure for additional
-  diagnostics. Other APIs do not guarantee `errno` content.
+* The header also defines `enum wc_rc` as a convenience alias for these result codes.
+* When `WC_HAVE_ERRNO != 0`, APIs may set `errno` on failure as a diagnostic.
+  Only the return code (and `err_out` where present) is the stable contract.
 
 ### Thread Safety
 
@@ -433,8 +438,10 @@ Per-instance memory and sizing limits:
     * Optional heap/static scan buffer (if `WC_STACK_BUFFER == 0`)
   * Does **not** count:
 
-    * The `wc` struct itself
-    * Arrays returned by `wc_results`
+    * The `wc` struct itself when allocated outside the internal allocator
+      (e.g., via `WC_MALLOC` or placed in `handle_buf`)
+    * Arrays returned by `wc_results` and `wc_topn`
+    * The `wc_stream` object returned by `wc_stream_open`
   * `0` = unlimited.
 * `strict_max_bytes`:
 
@@ -813,6 +820,8 @@ Freestanding / exotic toolchains:
 * `-DWC_USE_LIBC_QSORT=0`
 * `-DWC_HAVE_ERRNO=0`
 * `-DWC_NO_HEAP=1`
+* `-DWC_U32_T=your_uint32_type`
+* `-DWC_U64_T=your_uint64_type` (only needed with `WC_HASH_STRONG=1`)
 
 Tiny RAM profile:
 
@@ -1003,7 +1012,8 @@ clang -std=c99 -O1 -g -fsanitize=address,undefined,fuzzer \
 
 ### Hardening Guide
 
-- Enable keyed hashing: set `wc_limits.hash_seed` or build with `-DWC_HASH_STRONG=ON`.
+- Enable keyed hashing: set `wc_limits.hash_seed` or compile with `-DWC_HASH_STRONG=1`
+  (or, when using CMake, configure with `-DWC_HASH_STRONG=ON`).
 - Bound memory: use `wc_limits.max_bytes` (or `WC_MAX_BYTES`/`--max-bytes`) and `wc_reserve` for known workloads.
 - Clamp probe storms: set `wc_limits.max_probe` when linear probing must be bounded; the value is the maximum slots examined per operation (including the initial slot). Exhaustion allows one grow attempt in dynamic mode, then returns `WC_NOMEM` (duplicates still increment even when the cap is hit).
 - `wc_stream_open()` allocates a small `wc_stream` object via `WC_MALLOC` and returns a library-owned handle; close it with `wc_stream_close()`.
diff --git a/c-quality.sh b/c-quality.sh
index 178d0c4..000d895 100755
--- a/c-quality.sh
+++ b/c-quality.sh
@@ -93,6 +93,31 @@ else
 fi
 
 note "Running cppcheck (hard fail on warnings/perf/portability)..."
+cc_cmd="${CC:-cc}"
+read -r -a cc_argv <<< "$cc_cmd"
+cc_bin="${cc_argv[0]}"
+
+ptrdiff_max=""
+
+if command -v "$cc_bin" >/dev/null 2>&1; then
+  # Prefer compiler built-in (works without headers on GCC/Clang).
+  ptrdiff_max="$("${cc_argv[@]}" -dM -E - </dev/null 2>/dev/null \
+    | awk '/^#define __PTRDIFF_MAX__ / { $1=""; $2=""; sub(/^  */, ""); print; exit }')"
+
+  # Fallback: force PTRDIFF_MAX by including <stdint.h>.
+  if [[ -z "$ptrdiff_max" ]]; then
+    ptrdiff_max="$(printf '#include <stdint.h>\n' \
+      | "${cc_argv[@]}" -dM -E -xc - 2>/dev/null \
+      | awk '/^#define PTRDIFF_MAX / { $1=""; $2=""; sub(/^  */, ""); print; exit }')"
+  fi
+fi
+
+cppcheck_defines=()
+if [[ -n "$ptrdiff_max" ]]; then
+  cppcheck_defines+=("-DWC_PTRDIFF_MAX=$ptrdiff_max")
+else
+  fail "Could not determine PTRDIFF_MAX (no compiler, and no usable macro). Set WC_PTRDIFF_MAX manually for cppcheck."
+fi
 hard_args=(
   --check-level=exhaustive
   --enable=warning,performance,portability
@@ -103,7 +128,7 @@ hard_args=(
 )
 
 if cdb_dir="$(detect_cdb_dir)"; then
-  cppcheck "${hard_args[@]}" --project="$cdb_dir/$CDB"
+  cppcheck "${hard_args[@]}" "${cppcheck_defines[@]}" --project="$cdb_dir/$CDB"
 else
   files=()
   while IFS= read -r -d '' f; do
@@ -112,7 +137,7 @@ else
   if ((${#files[@]} == 0)); then
     note "No source files found; skipping cppcheck (hard)."
   else
-    printf '%s\0' "${files[@]}" | xargs -0 cppcheck "${hard_args[@]}" --language=c --std=c99 -I"$SRC_ROOT"
+    printf '%s\0' "${files[@]}" | xargs -0 cppcheck "${hard_args[@]}" "${cppcheck_defines[@]}" --language=c --std=c99 -I"$SRC_ROOT"
   fi
 fi
 
@@ -129,7 +154,7 @@ soft_args=(
 )
 
 if cdb_dir="$(detect_cdb_dir)"; then
-  cppcheck "${soft_args[@]}" --project="$cdb_dir/$CDB" || true
+  cppcheck "${soft_args[@]}" "${cppcheck_defines[@]}" --project="$cdb_dir/$CDB" || true
 else
   files=()
   while IFS= read -r -d '' f; do
@@ -138,7 +163,7 @@ else
   if ((${#files[@]} == 0)); then
     note "No source files found; skipping cppcheck (style)."
   else
-    printf '%s\0' "${files[@]}" | xargs -0 cppcheck "${soft_args[@]}" --language=c --std=c99 -I"$SRC_ROOT" || true
+    printf '%s\0' "${files[@]}" | xargs -0 cppcheck "${soft_args[@]}" "${cppcheck_defines[@]}" --language=c --std=c99 -I"$SRC_ROOT" || true
   fi
 fi
 
diff --git a/wc_test.c b/wc_test.c
index be1210c..087984f 100644
--- a/wc_test.c
+++ b/wc_test.c
@@ -575,34 +575,34 @@ static int test_limits_invalid_combinations(void)
     TEST("limits: invalid combinations rejected deterministically");
 
     /* static_buf without static_size */
-    lim = WC_LIMITS_INIT();
+    wc_limits_init(&lim);
     {
-        static unsigned char dummy[WC_TEST_ALIGN] = { 0 };
-        lim.static_buf = dummy;
+        static WC_TEST_STATIC_BUF(dummy, WC_TEST_ALIGN);
+        lim.static_buf = dummy.buf;
     }
     lim.static_size = 0;
     ASSERT(wc_open_ex(0, &lim, &rc) == NULL);
     ASSERT(rc == WC_EBADLIMITS);
 
     /* static_size without static_buf */
-    lim = WC_LIMITS_INIT();
+    wc_limits_init(&lim);
     lim.static_buf = NULL;
     lim.static_size = 64;
     ASSERT(wc_open_ex(0, &lim, &rc) == NULL);
     ASSERT(rc == WC_EBADLIMITS);
 
     /* handle_buf without handle_size */
-    lim = WC_LIMITS_INIT();
+    wc_limits_init(&lim);
     {
-        static unsigned char dummy2[WC_TEST_ALIGN] = { 0 };
-        lim.handle_buf = dummy2;
+        static WC_TEST_STATIC_BUF(dummy2, WC_TEST_ALIGN);
+        lim.handle_buf = dummy2.buf;
     }
     lim.handle_size = 0;
     ASSERT(wc_open_ex(0, &lim, &rc) == NULL);
     ASSERT(rc == WC_EBADLIMITS);
 
     /* handle_size without handle_buf */
-    lim = WC_LIMITS_INIT();
+    wc_limits_init(&lim);
     lim.handle_buf = NULL;
     lim.handle_size = wc_handle_size();
     ASSERT(wc_open_ex(0, &lim, &rc) == NULL);
@@ -624,7 +624,7 @@ static int test_handle_buf_static_buf_alias_rules(void)
     TEST("limits: handle_buf/static_buf alias rules");
 
     /* handle_buf inside static range but not equal to static_buf => reject */
-    lim = WC_LIMITS_INIT();
+    wc_limits_init(&lim);
     lim.static_buf = pool.buf;
     lim.static_size = sizeof pool.buf;
     lim.handle_buf = (void *)(pool.buf + WC_TEST_ALIGN);
@@ -634,7 +634,7 @@ static int test_handle_buf_static_buf_alias_rules(void)
     ASSERT(rc == WC_EBADLIMITS);
 
     /* handle_buf == static_buf => ok (counts against static budget) */
-    lim = WC_LIMITS_INIT();
+    wc_limits_init(&lim);
     lim.static_buf = pool.buf;
     lim.static_size = sizeof pool.buf;
     lim.handle_buf = pool.buf;
@@ -645,7 +645,7 @@ static int test_handle_buf_static_buf_alias_rules(void)
     wc_close(w);
 
     /* handle_buf separate from static_buf => ok */
-    lim = WC_LIMITS_INIT();
+    wc_limits_init(&lim);
     lim.static_buf = pool.buf;
     lim.static_size = sizeof pool.buf;
     lim.handle_buf = handle2.buf;
@@ -1183,7 +1183,7 @@ static int test_duplicates_survive_exhaustion_max_bytes(void)
 static int test_strict_max_bytes_blocks_peaks(void)
 {
     wc_limits base = WC_LIMITS_INIT();
-    wc_limits lim = WC_LIMITS_INIT();
+    wc_limits lim;
     const wc_build_config *info = wc_build_info();
     wc_stats st;
     wc *probe;
@@ -1924,9 +1924,9 @@ static int test_results_and_stream_not_counted_in_stats(void)
 static int test_scan_len_gt_ptrdiff_max_rejected(void)
 {
     wc *w;
-    size_t big = (size_t)PTRDIFF_MAX;
+    size_t big = (size_t)WC_PTRDIFF_MAX;
 
-    TEST("scan: len > PTRDIFF_MAX rejected");
+    TEST("scan: len > WC_PTRDIFF_MAX rejected");
 
     w = wc_open(0);
     ASSERT(w != NULL);
@@ -1946,9 +1946,9 @@ static int test_stream_len_gt_ptrdiff_max_rejected(void)
     wc *w;
     wc_stream *s;
     int rc = WC_OK;
-    size_t big = (size_t)PTRDIFF_MAX;
+    size_t big = (size_t)WC_PTRDIFF_MAX;
 
-    TEST("stream: len > PTRDIFF_MAX rejected");
+    TEST("stream: len > WC_PTRDIFF_MAX rejected");
 
     w = wc_open(0);
     ASSERT(w != NULL);
diff --git a/wordcount.c b/wordcount.c
index 0ddb0b7..8e2151f 100644
--- a/wordcount.c
+++ b/wordcount.c
@@ -20,13 +20,14 @@
 **     wc_add, wc_results, etc.). The CLI (wc_main.c) is responsible
 **     for streaming stdin and mmap-based file I/O.
 **
-**   - All internal allocations are routed through wc_alloc_state so
-**     memory limits and static-buffer mode are enforced consistently.
-**     The only exception is the caller-owned array returned by
-**     wc_results(), which is allocated via WC_MALLOC and explicitly
-**     documented as *not* counted against internal limits.
-**     The table-growth fast path may allocate via WC_MALLOC when the
-**     post-grow table fits the budget to avoid transient peaks; this is
+**   - All allocations that count against wc_limits budgets (table, arena blocks,
+**     heap scan buffer when enabled) are routed through wc_alloc_state.
+**     Allocations that are explicitly outside the wc allocator budget use
+**     WC_MALLOC/WC_FREE and are not counted: the wc handle when not placed in
+**     limits.handle_buf, arrays returned by wc_results()/wc_topn(), and the
+**     wc_stream object returned by wc_stream_open().
+**     Note: the internal table-growth fast path may allocate via WC_MALLOC when
+**     the post-grow table fits the budget to avoid transient peaks; it is still
 **     accounted after rehash.
 **
 **   - The implementation assumes a hosted C99 environment with:
@@ -213,7 +214,7 @@ static int wc_load_limits(wc_limits *out, const wc_limits *in)
     if (!out)
         return WC_ERROR;
     if (!in) {
-        *out = WC_LIMITS_INIT();
+        wc_limits_init(out);
         return WC_OK;
     }
 
@@ -246,8 +247,8 @@ WC_STATIC_ASSERT('A' == 65 && 'Z' == 90 && 'a' == 97 && 'z' == 122 &&
                  ascii_charset_required);
 
 WC_STATIC_ASSERT(CHAR_BIT == 8, char_bit_must_be_8);
-WC_STATIC_ASSERT(sizeof(wc_u32) * CHAR_BIT >= 32,
-                 wc_u32_must_be_at_least_32_bits);
+WC_STATIC_ASSERT(sizeof(wc_u32) * CHAR_BIT == 32, wc_u32_must_be_32_bits);
+WC_STATIC_ASSERT(((wc_u32)0 - (wc_u32)1) > (wc_u32)0, wc_u32_must_be_unsigned);
 
 WC_STATIC_ASSERT(WC_MAX_WORD >= 4u, wc_max_word_must_be_at_least_4);
 WC_STATIC_ASSERT(WC_MIN_INIT_CAP >= 1u, wc_min_init_cap_must_be_positive);
@@ -872,8 +873,8 @@ fnv32(const char *s,
 #endif /* !WC_HASH_STRONG */
 
 #if WC_BOOL(WC_HASH_STRONG)
-WC_STATIC_ASSERT(sizeof(wc_u64) * CHAR_BIT >= 64,
-                 wc_u64_must_be_at_least_64_bits);
+WC_STATIC_ASSERT(sizeof(wc_u64) * CHAR_BIT == 64, wc_u64_must_be_64_bits);
+WC_STATIC_ASSERT(((wc_u64)0 - (wc_u64)1) > (wc_u64)0, wc_u64_must_be_unsigned);
 static wc_u64 sip_rotl(wc_u64 v, int b)
 {
     return (wc_u64)((v << b) | (v >> (wc_u64)(64 - b)));
@@ -1383,7 +1384,7 @@ wc *wc_open_ex(size_t max_word, const wc_limits *limits, int *err_out)
     if (!limits) {
         if (err_out)
             *err_out = WC_EBADLIMITS;
-        WC_SET_ERRNO(ENOMEM);
+        WC_SET_ERRNO(EINVAL);
         return NULL;
     }
 #endif
@@ -1436,7 +1437,7 @@ wc *wc_open_ex(size_t max_word, const wc_limits *limits, int *err_out)
 #if WC_BOOL(WC_NO_HEAP)
         if (!lim_static_buf || lim_static_size == 0) {
             err = WC_EBADLIMITS;
-            WC_SET_ERRNO(ENOMEM);
+            WC_SET_ERRNO(EINVAL);
             goto fail;
         }
         /* In no-heap builds, default the handle to the static buffer if omitted. */
@@ -2578,7 +2579,7 @@ wc_stream *wc_stream_open(wc *w, int *err_out)
 #if WC_BOOL(WC_NO_HEAP)
     (void)w;
     if (err_out)
-        *err_out = WC_EBADLIMITS;
+        *err_out = WC_NOMEM;
     WC_SET_ERRNO(ENOMEM);
     return NULL;
 #else
diff --git a/wordcount.h b/wordcount.h
index c7ee774..e1689b9 100644
--- a/wordcount.h
+++ b/wordcount.h
@@ -38,13 +38,16 @@
 **   default ASCII build. With WC_ASCII_ONLY=0, tokenization follows the
 **   caller's C locale.
 **
-**   Public API: wc_open_ex, wc_open, wc_close, wc_handle_size, wc_add,
-**   wc_add_n, wc_add_norm_n, wc_scan, wc_total, wc_unique,
-**   wc_results, wc_results_free, wc_results_into, wc_topn,
-**   wc_topn_into, wc_reserve, cursor APIs, wc_errstr, wc_version,
-**   wc_build_info, wc_get_stats, wc_validate, wc_stream_open,
-**   wc_stream_size, wc_stream_open_inplace, wc_stream_scan_ex,
-**   wc_stream_scan, wc_stream_finish, wc_stream_close.
+**   Public API functions: wc_open_ex, wc_open, wc_close, wc_handle_size, wc_add,
+**     wc_add_n, wc_add_norm_n, wc_scan, wc_total, wc_unique, wc_results,
+**     wc_results_into, wc_results_free, wc_topn, wc_topn_into, wc_reserve,
+**     wc_cursor_init, wc_cursor_next, wc_errstr, wc_version, wc_build_info,
+**     wc_get_stats, wc_validate, wc_stream_open, wc_stream_size,
+**     wc_stream_open_inplace, wc_stream_scan_ex, wc_stream_scan,
+**     wc_stream_finish, wc_stream_close.
+**
+**   Public API helpers/macros: wc_limits_init, WC_LIMITS_INIT,
+**     WC_BUILD_CONFIG_INIT, WC_STATIC_BUFFER.
 **
 ** STABILITY
 **
@@ -266,12 +269,16 @@
 #endif
 #endif
 
+/* WC_PTRDIFF_MAX: override only on non-standard platforms missing PTRDIFF_MAX. */
 #ifndef WC_PTRDIFF_MAX
+/* Prefer the standard PTRDIFF_MAX from <stdint.h> (C99). */
+#if !defined(PTRDIFF_MAX)
+#include <stdint.h>
+#endif
 #if defined(PTRDIFF_MAX)
 #define WC_PTRDIFF_MAX PTRDIFF_MAX
 #else
-#define WC_PTRDIFF_MAX \
-    ((ptrdiff_t)((((size_t)1) << (sizeof(ptrdiff_t) * CHAR_BIT - 1)) - 1))
+#error "PTRDIFF_MAX unavailable; define WC_PTRDIFF_MAX for this platform."
 #endif
 #endif
 
@@ -327,7 +334,10 @@ extern "C" {
 #endif
 #endif
 
-/* Warn on ignored results where supported. */
+/* Annotation macros:
+   WC_RESTRICT  - expands to C99 restrict when available, else empty.
+   WC_NODISCARD - expands to a warn-unused-result attribute when supported.
+   WC_WUR       - internal shorthand used on public declarations. */
 #ifndef WC_NODISCARD
 #if defined(__GNUC__) || defined(__clang__)
 #define WC_NODISCARD __attribute__((warn_unused_result))
@@ -451,6 +461,14 @@ extern "C" {
 
 /* --- Public types ----------------------------------------------------- */
 
+/*
+** WC_U32_T / WC_U64_T (advanced):
+**   Override the internal hashing integer types.
+**   Requirements (enforced by compile-time asserts in wordcount.c):
+**     - WC_U32_T must be an unsigned integer type of exactly 32 bits.
+**     - If WC_HASH_STRONG=1, WC_U64_T must be an unsigned integer type of exactly 64 bits.
+**   Defaults use <stdint.h> uint32_t/uint64_t.
+*/
 #ifndef WC_U32_T
 #include <stdint.h>
 #define WC_U32_T uint32_t
@@ -481,9 +499,12 @@ typedef struct wc wc;
 **       - the arena blocks used for word storage
 **       - the optional heap scan buffer when WC_STACK_BUFFER==0
 **
-**     The wc handle itself (the struct wc) and any arrays returned
-**     by wc_results() are NOT counted, since their lifetime and
-**     ownership are under the caller's control. 0 = unlimited.
+**     Not counted against this limit:
+**       - the wc handle itself when allocated outside the internal allocator
+**         (e.g., via WC_MALLOC, or placed by the caller in handle_buf),
+**       - arrays returned by wc_results() and wc_topn(),
+**       - the wc_stream object returned by wc_stream_open().
+**     These allocations are outside the wc allocator budget. 0 = unlimited.
 **
 **   init_cap:
 **     Initial hash table capacity (number of slots). Must be > 0.
@@ -520,9 +541,10 @@ typedef struct wc wc;
 **         and is clamped to static_size when computing initial
 **         sizing.
 **
-**     The wc handle itself and any arrays returned by wc_results()
-**     are still allocated via WC_MALLOC/WC_FREE. On very small
-**     systems, redirect those macros to your own allocator.
+**     The wc handle itself is allocated via WC_MALLOC/WC_FREE unless the caller
+**     provides limits.handle_buf. When WC_NO_HEAP=0, arrays returned by
+**     wc_results()/wc_topn() and stream objects returned by wc_stream_open()
+**     are allocated via WC_MALLOC/WC_FREE.
 **
 ** On small systems, set max_bytes or static_size to a fixed budget
 ** and leave the others at 0 to let the library derive conservative
@@ -588,14 +610,29 @@ typedef struct wc_limits {
     size_t handle_size;
 } wc_limits;
 
+/* If you append fields to wc_limits, update the C++ initializer list here. */
 #ifdef __cplusplus
-#define WC_LIMITS_INIT()  \
-    wc_limits             \
-    {                     \
-        sizeof(wc_limits) \
+/* C++ aggregate init: fully initialize to avoid -Wmissing-field-initializers. */
+#define WC_LIMITS_INIT()                          \
+    {                                             \
+        sizeof(wc_limits), /* struct_size */      \
+                0,         /* max_bytes */        \
+                0,         /* strict_max_bytes */ \
+                0,         /* init_cap */         \
+                0,         /* block_size */       \
+                0,         /* static_buf */       \
+                0,         /* static_size */      \
+                0ul,       /* hash_seed */        \
+                0,         /* max_probe */        \
+                0,         /* handle_buf */       \
+                0          /* handle_size */      \
     }
 #else
-#define WC_LIMITS_INIT() ((wc_limits){ .struct_size = sizeof(wc_limits) })
+/* C99 constant initializer (valid for static storage). */
+#define WC_LIMITS_INIT()                 \
+    {                                    \
+        .struct_size = sizeof(wc_limits) \
+    }
 #endif
 
 /* Recommended initializer (sets struct_size and zeroes all fields). */
@@ -654,15 +691,27 @@ typedef struct wc_build_config {
     size_t sizeof_wc_limits;
 } wc_build_config;
 
+/* WC_BUILD_CONFIG_INIT: constant initializer for static storage in C;
+   in C++ we fully initialize to avoid -Wmissing-field-initializers.
+   If you append fields to wc_build_config, update the C++ initializer list. */
 #ifdef __cplusplus
-#define WC_BUILD_CONFIG_INIT()  \
-    wc_build_config             \
-    {                           \
-        sizeof(wc_build_config) \
+#define WC_BUILD_CONFIG_INIT()                          \
+    {                                                   \
+        sizeof(wc_build_config), /* struct_size */      \
+                0ul,             /* version_number */   \
+                0,               /* max_word */         \
+                0,               /* min_init_cap */     \
+                0,               /* min_block_sz */     \
+                0,               /* stack_buffer */     \
+                0,               /* sizeof_wc */        \
+                0,               /* sizeof_slot */      \
+                0                /* sizeof_wc_limits */ \
     }
 #else
-#define WC_BUILD_CONFIG_INIT() \
-    ((wc_build_config){ .struct_size = sizeof(wc_build_config) })
+#define WC_BUILD_CONFIG_INIT()                 \
+    {                                          \
+        .struct_size = sizeof(wc_build_config) \
+    }
 #endif
 
 /*
@@ -676,6 +725,12 @@ typedef struct wc_stats {
     size_t arena_blocks;
 } wc_stats;
 
+/*
+** wc_rc:
+**   Convenience enum alias for result codes. Values match WC_OK/WC_ERROR/WC_NOMEM
+**   and the extended codes (WC_EBADLIMITS/WC_EALIGN/WC_EBUSY).
+**   You may use either the macros or this enum; they are equivalent.
+*/
 typedef enum wc_rc {
     WC_RC_OK = WC_OK,
     WC_RC_ERROR = WC_ERROR,
@@ -772,6 +827,11 @@ WC_API void wc_results_free(wc_word *r);
 
 /* Top-N helper (count desc, then lexicographic).
 **
+** Note: The output array returned via *out is allocated via WC_MALLOC and is
+** not counted against max_bytes in wc_limits, nor against any static buffer
+** provided via wc_limits.static_buf, since its lifetime is under the caller's
+** control.
+**
 ** When WC_NO_HEAP=1, returns WC_NOMEM unconditionally; use
 ** wc_topn_into with caller-supplied storage instead.
 */
@@ -863,6 +923,17 @@ WC_API WC_WUR int wc_validate(const wc *w);
 */
 typedef struct wc_stream wc_stream;
 
+/*
+** wc_stream_open:
+**   Allocates a wc_stream object via WC_MALLOC (not counted against wc_limits).
+**   Returns NULL on failure and stores a code in *err_out when provided.
+**   When WC_NO_HEAP=1, always returns NULL with *err_out = WC_NOMEM; use
+**   wc_stream_open_inplace().
+**
+** wc_stream_open_inplace:
+**   Uses caller-provided storage. On mem_size < wc_stream_size(w), fails with
+**   WC_NOMEM (and errno may be set when WC_HAVE_ERRNO!=0).
+*/
 WC_API wc_stream *wc_stream_open(wc *w, int *err_out);
 WC_API size_t wc_stream_size(const wc *w);
 WC_API wc_stream *
